apiVersion: v1
kind: Service
metadata:
  name: dataspace-issuer
  namespace: gxdch-issuer
spec:
  selector:
    app: dataspace-issuer-server
  ports:
    - name: web
      port: 80
      targetPort: web
